node_modules/
dist/
.DS_Store
.env
.env.*
# Allow .env.example template files inside golden-config fixtures —
# they're test data, not runtime secrets.
!tests/fixtures/**/.env.example
*.log

# IDE
.idea/
.vscode/
*.swp
*.swo

# Build outputs
out/

# Test coverage reports (vitest --coverage / npm run test:coverage)
coverage/

# Session backend runtime data (json-file backend default dir)
.embediq/

# Claude Code session lock files — runtime artifacts of the CLI tool,
# not part of the project's own .claude/ tracked configuration.
.claude/scheduled_tasks.lock

# Evaluation run outputs (transient — N/M/K/T snapshots live in STATUS.md instead)
reports/
