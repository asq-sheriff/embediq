// A record: { name, mrn, dob, coverage }. All four are required.
//
// validateEnrollment(record):
//   - if every required field is present, return true;
//   - if a required field is missing, throw an Error with enough context to
//     debug which record and which field failed.
// Follow this project's conventions.
export function validateEnrollment(record) {
  // TODO: implement
  throw new Error('not implemented');
}
