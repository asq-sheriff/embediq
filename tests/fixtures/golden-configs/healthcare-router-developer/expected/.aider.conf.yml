# Aider config — generated by EmbedIQ
# Hybrid: hosted model for code edits, local Ollama for lightweight tasks (commit messages, repo-map summaries).
model: anthropic/claude-sonnet-4-6
weak-model: ollama/qwen2.5-coder:32b
# Compliance frameworks active: hipaa

# Lint + test on every change so the generated code stays green.
auto-lint: true
lint-cmd: npm run lint
auto-test: true
test-cmd: npm test

# Conservative defaults — no auto-commit, dry-run on dangerous ops.
auto-commits: false
dirty-commits: false
attribute-author: false
attribute-committer: false
